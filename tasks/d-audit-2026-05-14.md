# §D Bug Audit — 2026-05-14

**Method:** Read current code against the 16 items claimed open in `docs/LAUNCH-CHECKLIST.md` §D. Spot-verified every "FIXED" claim from the Explore-agent run.

**Conclusion:** the checklist is significantly out of date. **8 of 16 items are already fixed** in code; **5 are real launch blockers**; 3 are deferrable or unclear.

## Verdict per bug

| ID | Claim | Current state | Action |
|---|---|---|---|
| **D-C1** | LD `course_completed` sync missing | ✅ **FIXED** — `EditionCompletion.php:154-155, 184-185` call `learndash_process_mark_complete()` | Close in checklist |
| **D-C2** | Deprecated `current_time('timestamp')` | ⚠️ **REAL** — 2 calls at `UserDashboardService.php:728-729` | Fix (2-line change) |
| **D-C3** | Cache not cleared on task update | ✅ **FIXED** — `RegistrationRepository.php:619` calls `clearCache()` after `updateCompletionTasks()` | Close |
| **D-C4** | `Withdrawn` enum mismatch | ✅ **FIXED** — `RegistrationStatus.php:18` defines `Withdrawn = 'withdrawn'`, label `'Uitgetrokken'`, DB enum aligned | Close |
| **D-C5** | DI coupling — refactor for testability | ⚠️ **architectural debt, not a bug** — `CompletionTaskHandler` uses `ntdst_get()` 7×; matches the thin-handler pattern documented in CLAUDE.md. Not blocking. | Drop from launch list |
| **D-A1** | Cascade delete missing | ✅ **FIXED** — `EditionService.php:301-341` cascades to sessions + registrations + attendance | Close |
| **D-A2** | Orphan `session_registrations` rows | ✅ **N/A** — table doesn't exist in DB; zero refs in code. Dead concern. | Close |
| **D-A3** | Attendance count semantic inconsistency | ✅ **FIXED** — both `countAttended()` and `getPresentUserIds()` use `AttendanceStatus::attendedValues()` | Close |
| **D-T1** | 7 footer pages 404 | ⚠️ **REAL but smaller** — actually **6** broken links (`/agenda/`, `/contact/`, `/faq/`, `/over-ons/`, `/privacy/`, `/voorwaarden/`). Only `privacy-policy` page exists (draft, wrong slug). | Fix — create 6 pages OR remove from footer |
| **D-T2** | LD ProPanel console notice | 🤷 **UNCLEAR** — zero ProPanel refs in theme; if notice appears it's plugin-side. Can't verify without loading the site logged-in as admin. | Skip until reproduced |
| **D-T3** | 11 unimplemented shortcodes | 🤷 **VAGUE** — checklist never enumerated which 11. Grep shows 4-7 registered, but "missing" list isn't documented. | Drop until specified |
| **D-G1** | Anonymise on user delete | 🚨 **REAL P0** — zero `delete_user` hooks in stride-core; no `UserLifecycleService`; users hard-deleted → orphans registrations, quotes, certificates | Fix (new service + hook + meta) |
| **D-G2** | EditionRegistrationMetabox renders anonymised users | 🚨 **REAL P0 (depends on D-G1)** — line 151–153 silently `continue`s on missing user. Currently shows nothing; once D-G1 lands, must show "verwijderd" badge | Fix after D-G1 |
| **D-G3** | Cleanup CLI `wp stride anonymise-orphans` | 🚨 **REAL P1 (depends on D-G1)** — no command exists. Needed for dev DB safety net. | Fix with D-G1 |
| **D-Cap1** | Pending registrations hold capacity | ⚠️ **REAL P1** — `EditionService.php:98` SQL counts `pending + confirmed + completed`. Abandoned pendings hold Volzet forever. | Decide: stale-pending auto-expire (24-48h) OR accept + document |
| **D-Cap2** | Status doesn't recompute on capacity edit | ⚠️ **REAL P2** — no `recomputeStatus()` method, no CLI. Real edge case for admin bulk import. | Defer or build (~30 LOC) |

## What's actually blocking launch

**P0 (5):**
1. **D-G1 + D-G2 + D-G3** — GDPR anonymisation (one bundle of work, ~200 LOC). Without this, deleting a user via WP admin orphans real data.
2. **D-T1** — 6 broken footer links on the public site. Either create the pages or remove the links.

**P1 (2):**
3. **D-C2** — 2 deprecated `current_time('timestamp')` calls. Trivial fix.
4. **D-Cap1** — Decide policy on stale-pending registrations.

**Deferrable / unclear (3):**
- D-C5 (DI debt, not a bug)
- D-T2 (ProPanel — unreproducible)
- D-T3 (vague "11 shortcodes")
- D-Cap2 (P2 edge case)

## What this means for §D in LAUNCH-CHECKLIST.md

The "11 deferred bugs" framing is misleading. The real launch blockers in §D are:
- 1 GDPR bundle (D-G1/G2/G3) — biggest piece
- 1 broken-footer cleanup (D-T1) — small but visible
- 1 deprecated-API cleanup (D-C2) — trivial
- 1 stale-pending decision (D-Cap1) — policy question

8 items can be marked done. 3 should be dropped or downgraded.
